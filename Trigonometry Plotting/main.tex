\documentclass{article}  %ignore this line in main file

% add this packages in the top part of main file
%add from here to
\usepackage[margin=0.5in]{geometry}
\usepackage[utf8]{inputenc}
\usepackage{comment}

\usepackage{pgfplots}
\pgfplotsset{width=10cm, compat=1.9}
\documentclass[border=2mm,tikz]{standalone}
\usetikzlibrary{datavisualization}
% to, in the library part

\usepackage{pgfplots} %remove this line in main file
\begin{document} % %remove this line in main file

% add from below line in the exact place where you want to add the graph

\begin{tikzpicture}
  \begin{axis}[
  axis x line=center,
  axis y line=center,
  xtick={-5,-4,...,5},
  ytick={-5,-4,...,5},
  xlabel={$x$},
  ylabel={$y$},
  xlabel style={below right},
  -xlabel style={below left},
  ylabel style={above left},
  -ylabel style={above right},
  xmin=-5.5,
  xmax=5.5,
  ymin=-5.5,
  ymax=5.5]
]
  
    \addplot [smooth,mark=*] coordinates {
      (0,  0)
      (1,  1)
      (2,  0)
      (3, -1)
      (4, 0)
    };
    %\addlegendentry{$f(x)$} % means what graph you are plotting
  \end{axis}
\end{tikzpicture}
\end{document} % %remove this line in main file